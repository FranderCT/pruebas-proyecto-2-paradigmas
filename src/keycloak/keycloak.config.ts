// import Keycloak from 'keycloak-js';

// const keycloak = new Keycloak({
//   url: 'http://localhost:8080/realms/Paradigmas/account/', // URL de Keycloak
//   realm: 'Paradigmas', // El nombre de tu realm en Keycloak
//   clientId: 'dashboard', // Tu Client ID en Keycloak
// });

// keycloak.init({ onLoad: 'login-required', pkceMethod: 'S256', enableLogging: true })
//   .then(authenticated => {
//     console.log(authenticated ? 'Authenticated' : 'Not authenticated');
//   })
//   .catch(error => {
//     console.error('Error during Keycloak initialization', error);
//   });
